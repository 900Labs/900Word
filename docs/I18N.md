# Internationalization

900Word will use structured translation files for UI strings and keep document content separate from UI locale.

Initial language work:

- English source strings.
- RTL layout smoke path.
- Locale-aware dates only where user-visible.
- Translation contribution guidance before accepting community translations.

Do not hard-code user-facing strings in Rust or Svelte components without adding them to the translation surface.
